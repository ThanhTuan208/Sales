@using CRUD_asp.netMVC.Models.Product
@model Brand

@{
    ViewData["Title"] = "Detail Brand";
}

<div class="product-detail-container">
    <h1 class="text-center" style="color: #2c3e50; margin-bottom: 20px;">Thương hiệu chi tiết</h1>
    <hr style="border-color: #ddd;" />
    <div class="product-card">
        <div class="product-image">
            <img src="@Url.Content($"~/{Model.PicturePath}")" alt="" />
        </div>
        <div class="product-info">
            <dl>
                <dt>Mã thương hiệu</dt>
                <dd>@Html.DisplayFor(model => model.ID)</dd>
                <dt>Tên thương hiệu</dt>
                <dd>@Html.DisplayFor(model => model.Name)</dd>
                <dt>Mô tả</dt>
                <dd>@Html.DisplayFor(model => model.Description)</dd>
            </dl>
        </div>
    </div>
    <div class="action-buttons">
        @* <a asp-action="EditProduct" asp-route-id="@Model.ID">cập nhật</a>  *@
        <a asp-action="BrandList">Qiuay lại</a>
    </div>
</div>